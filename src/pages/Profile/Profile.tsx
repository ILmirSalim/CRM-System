import React from 'react';
import styles from './Profile.module.scss'

export const Profile: React.FC = () => {
    return <div className={styles.profile}>
        Привет! Это страница профиля!
    </div>
}
